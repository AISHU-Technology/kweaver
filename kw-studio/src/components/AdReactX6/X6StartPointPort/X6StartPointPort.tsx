import React from 'react';
import IconFont from '@/components/IconFont';

const X6StartPointPort: React.FC = props => {
  return <IconFont type="icon-mouse_add" style={{ fontSize: 10, position: 'absolute', color: '#BFBFBF' }} />;
};

export default X6StartPointPort;
