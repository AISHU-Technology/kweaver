export type DataItem = {
  model_id: string;
  model_series: string;
  model_name: string;
  model: string;
  model_api: string;
  create_by: string;
  update_by: string;
  create_time: number;
  update_time: number;
};

export type { TableState } from './enums';
