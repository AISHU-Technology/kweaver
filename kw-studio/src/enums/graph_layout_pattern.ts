const COMMON = 'common';
const TREE = 'tree';

const PATTERN_OBJECT = {
  COMMON,
  TREE
};

const GRAPH_LAYOUT_PATTERN = {
  PATTERN_OBJECT,
  COMMON,
  TREE
};

export default GRAPH_LAYOUT_PATTERN;
