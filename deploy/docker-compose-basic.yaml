version: '3.7'

services:
  kw-nginx:
    container_name: kw-nginx
    image: nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/log/error.log:/var/log/nginx/error.log
    ports:
      - "80:80"
    environment:
      - NGINX_PORT=80
    networks:
      - kw-network
    depends_on:
      - kw-studio
      - kw-builder

  kw-studio:
    container_name: kw-studio
    image: acr.aishu.cn/ad/kw-studio:2.0.0.5
    restart: always
    volumes:
      - ./kwconfig/:/etc/studio/:ro
    networks:
      - kw-network
    ports:
      - '6800:6800'
    expose:
      - '6800'

  kw-builder:
    container_name: kw-builder
    image: acr.aishu.cn/ad/kw-builder:2.0.0.5
    restart: always
    volumes:
      - ./kwconfig/:/etc/builder/:ro
    networks:
      - kw-network
    ports:
      - '6475:6475'
    expose:
      - '6475'
      - '6485'
      - '6488'
    depends_on:
      - kw-studio

  kw-algserver:
    container_name: kw-algserver
    image: acr.aishu.cn/ad/kw-algserver:2.0.0.5
    restart: always
    volumes:
      - ./kwconfig/:/etc/builder/:ro
    networks:
      - kw-network
    ports:
      - '8080:8080'
    depends_on:
      - kw-mariadb

  kw-engine:
    container_name: kw-engine
    image: acr.aishu.cn/ad/kw-engine:2.0.0.5
    restart: always
    volumes:
      - ./kwconfig/:/etc/builder/:ro
    networks:
      - kw-network
    ports:
      - '6474:6474'
    depends_on:
      - kw-mariadb
      - kw-builder
        
networks:
  kw-network:
    driver: bridge

volumes:
  db_data: